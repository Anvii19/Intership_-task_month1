Work With File Formats
✅ Example: Read CSV File
📄 Code
import csv

with open("data.csv", "r") as file:
    reader = csv.reader(file)
    for row in reader:
        print(row)
🧠 Explanation

import csv → CSV module

csv.reader() → reads rows

Each row is a list

📌 Concepts Covered: CSV, Modules, File Reading
